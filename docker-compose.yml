# docker-compose.yml
version: '3.8'

services:
  mcp-server:
    build: .
    container_name: mcp-api-server
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped